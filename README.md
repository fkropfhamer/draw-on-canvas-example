# Test project for draw-on-canvas

clone https://github.com/fkropfhamer/draw-on-canvas

move into `draw-on-canvas` folder
execute `npm link`
go into example folder
execute `npm link draw-on-canvas`

run `npm run watch`
open `./public/index.html` in Browser.
 